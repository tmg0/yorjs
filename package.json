{
  "name": "yorjs",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "pnpm -r --filter=./packages/* dev",
    "dev:core": "pnpm -C ./packages/core dev",
    "dev:shared": "pnpm -C ./packages/shared dev",
    "build": "pnpm -r --filter=./packages/* build",
    "upgrade:core": "pnpm -C ./packages/core upgrade",
    "ci-publish:core": "pnpm -C ./packages/core ci-publish",
    "ci-publish:shared": "pnpm -C ./packages/shared ci-publish",
    "test": "vitest"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@nuxtjs/eslint-config-typescript": "^12.0.0",
    "eslint": "^8.24.0",
    "tsup": "^6.5.0",
    "typescript": "^4.8.2",
    "vitest": "^0.28.5"
  },
  "volta": {
    "node": "18.12.1"
  }
}
