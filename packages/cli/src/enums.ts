export enum Yor {
  MODULE = 'module',
  INTERFACE ='interface',
  PROVIDER = 'provider',
  CONTROLLER = 'controller'
}

export enum YorAlias {
  MODULE = 'm',
  INTERFACE = 'i',
  PROVIDER = 'p',
  CONTROLLER = 'c'
}
